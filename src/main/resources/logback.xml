<?xml version="1.0" encoding="UTF-8"?>

<configuration>
    <include resource="org/springframework/boot/logging/logback/defaults.xml" />
    <property name="LOG_FILE" value="var/log/app.log" />
    <appender name="console" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} %mdc [%-5level] [%t] %c{1} - %msg%n</pattern>
        </encoder>
    </appender>

    <appender name="ROLLING-FILE"
              class="ch.qos.logback.core.rolling.RollingFileAppender">
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} %mdc [%-5level] [%t] %c{1} - %msg%n</pattern>
        </encoder>
        <file>${LOG_FILE}</file>
        <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
            <fileNamePattern>${LOG_FILE}.%d{yyyy-MM-dd}.%i.gz</fileNamePattern>
            <timeBasedFileNamingAndTriggeringPolicy
                    class="ch.qos.logback.core.rolling.SizeAndTimeBasedFNATP">
                <maxFileSize>20MB</maxFileSize>
            </timeBasedFileNamingAndTriggeringPolicy>
            <maxHistory>5</maxHistory>
            <totalSizeCap>100MB</totalSizeCap>
        </rollingPolicy>
        <filter class="ch.qos.logback.classic.filter.ThresholdFilter"> <level>debug</level>
        </filter>
    </appender>

    <springProfile name="!prod">

        <root level="all">
            <appender-ref ref="console" />
        </root>
        <logger name="com.digitap" additivity="false">
            <level value="all" />
            <appender-ref ref="ROLLING-FILE" />
            <appender-ref ref="console" />
        </logger>

    </springProfile>

    <springProfile name="prod">
        <!-- configuration to be enabled when the "production" profile is active -->
        <root level="INFO">
            <appender-ref ref="console" />
        </root>
        <logger name="com.digitap" additivity="false">
            <level value="INFO" />
            <appender-ref ref="ROLLING-FILE" />
            <appender-ref ref="console" />
        </logger>
    </springProfile>
</configuration>